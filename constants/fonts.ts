import { Karla, Righteous } from "next/font/google";

export const righteous = Righteous({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const karla = Karla({ subsets: ["latin"], display: "swap" });
